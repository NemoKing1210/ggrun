/**
 * Расширяет литеральные типы словаря до string: значения en-словарей
 * объявлены as const, но ru/uk-версии должны быть типобезопасны
 * относительно структуры (те же ключи), а не тех же литералов.
 */
export type Widen<T> = {
  [K in keyof T]: T[K] extends string ? string : Widen<T[K]>;
};
